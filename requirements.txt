# LEO Satellite Coverage Risk Analysis — Python Dependencies
# Python 3.11+
#
# Install: pip install -r requirements.txt
# For reproducible builds (e.g. CI/production), pin versions or use a lock file.

# --- AI / Agent ---
anthropic

# --- Data processing ---
pandas
numpy

# --- Geospatial ---
geopandas
rasterio
shapely
pygris          # Census TIGER/Line county data (county FIPS → NAMELSAD name)
us              # US state FIPS / name / abbreviation lookups

# --- Visualization ---
matplotlib
seaborn
folium
plotly              # Interactive charts in admin dashboard

# --- Reporting / spreadsheet generation ---
openpyxl

# --- Web UI (Phase 7) ---
flask
markdown

# --- Utilities ---
python-dotenv
tqdm
requests
reverse_geocoder
geopy              # Address geocoding via Nominatim (OSM, free, no API key)

# --- Testing ---
pytest
