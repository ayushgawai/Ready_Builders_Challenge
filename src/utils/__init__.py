"""
Shared utilities for the LEO Satellite Coverage Risk pipeline.

Modules:
  logging_config  — centralised logging setup used by all entry points
  geo_utils       — geographic helper functions reused across pipeline modules
"""
